<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".activities.SignUpActivity">

    <EditText
        android:id="@+id/sign_up_emailInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="149dp"
        android:layout_marginBottom="24dp"
        android:ems="10"
        android:hint="@string/email"
        android:inputType="textEmailAddress"
        app:layout_constraintBottom_toTopOf="@+id/sign_up_passwordInput"
        app:layout_constraintStart_toStartOf="@+id/sign_up_passwordInput"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/sign_up_passwordInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="2dp"
        android:layout_marginBottom="20dp"
        android:ems="10"
        android:hint="@string/password"
        android:inputType="textPassword"
        app:layout_constraintBottom_toTopOf="@+id/sign_up_confirmPasswordInput"
        app:layout_constraintEnd_toEndOf="@+id/sign_up_confirmPasswordInput"
        app:layout_constraintTop_toBottomOf="@+id/sign_up_emailInput" />

    <EditText
        android:id="@+id/sign_up_confirmPasswordInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginBottom="16dp"
        android:ems="10"
        android:hint="@string/confirm_password"
        android:inputType="textPassword"
        app:layout_constraintBottom_toTopOf="@+id/sign_up_signUpButton"
        app:layout_constraintStart_toStartOf="@+id/sign_up_signUpButton"
        app:layout_constraintTop_toBottomOf="@+id/sign_up_passwordInput" />

    <Button
        android:id="@+id/sign_up_signUpButton"
        android:layout_width="216dp"
        android:layout_height="0dp"
        android:layout_marginBottom="343dp"
        android:text="@string/sign_up"
        android:textColor="@android:color/holo_blue_light"
        app:backgroundTint="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/sign_up_confirmPasswordInput" />

</androidx.constraintlayout.widget.ConstraintLayout>